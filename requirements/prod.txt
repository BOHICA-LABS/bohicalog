more_itertools == 9.0.0 # Missing itertools from the standard library you didn't know you needed
tqdm == 4.64.1 # A fast, extensible progress bar for Python and CLI
requests == 2.28.1 # Python HTTP for Humans
cloudscraper == 1.2.66 # A Python module to bypass Cloudflare's anti-bot page
retry == 0.9.2 # A simple decorator for retrying a function call
click == 8.1.3 # Composable command line interface toolkit
more_click == 0.1.2 # A collection of useful click extensions
colorama == 0.4.4 ;sys_platform=="win32" # Cross-platform colored terminal text